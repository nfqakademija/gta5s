{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, { 'action': path('fos_user_profile_edit'), 'attr': { 'class': 'fos_user_profile_edit' } }) }}
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'profile.edit.submit'|trans }}" />
    </div>
{{ form_end(form) }}

<hr/>

<div class="profile-avatar-change">
    <p>Keisti avatarą</p>
        <div class="col-sm-6">
            <form
                action="{{ oneup_uploader_endpoint('avatars') }}"
                class="dropzone"
                style="
                    width:200px;
                    height:200px;
                    border:4px dashed black
                ">
            </form>
        </div>
        <div class="col-sm-6">
            <img src="{{ asset(app.user.getAvatar().getFileName()) }}"/>
        </div>

</div>
